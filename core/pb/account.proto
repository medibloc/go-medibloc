syntax = "proto3";
package corepb;

message Account {
    bytes address = 1;
    bytes balance = 2;
    uint64 nonce = 3;
    bytes observations_hash = 4;
    repeated bytes writers = 5;
    repeated bytes records = 6;
}
